export function isMaster(role?: string) {
  return role === "MASTER";
}

export function isMentor(role?: string) {
  return role === "MENTOR";
}

export function isAprendiz(role?: string) {
  return role === "APRENDIZ";
}
